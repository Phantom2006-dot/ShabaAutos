# Gemini API Secret (Server-side only)
GEMINI_API_KEY=

# PostgreSQL / Neon Database Connection String
DATABASE_URL=

# Persistent SQLite Database File Path (Fallback if DATABASE_URL is not set)
SQLITE_DB_PATH=

# Node Environment
NODE_ENV=development
